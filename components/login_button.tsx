import React from 'react';
import styled from 'styled-components';

function LoginButton() {
  return <Button>Google 계정으로 시작하기</Button>;
}

export default LoginButton;

const Button = styled.button``;
